# 更新日志

## v0.2.0-test - 2026-08-24

### 主要内容更新

- 完整汉化第 51 款隐藏内容《瘴气塔》，覆盖场景叙事、人物对话、物件调查、任务提示、结局与职员表。
- 保留隐藏内容发现链所需的英文身份、终端命令、内部名、八位代码与文件扩展名，避免中文化破坏原版解谜。
- 中文终端遵循官方日文本地化规则：自然语言使用中文，玩家实际输入的协议保持原样，日期代码采用东亚年月日顺序。

### 按游戏

#### 《瘴气塔》

- 新增 547 条逐条翻译和自校的正文，正式中文名定为《瘴气塔》。
- 保留 `GREGORY MILK`、`GREG-MILK`、`STEN MURZBACH` 等参与英文拼写谜题的身份文本，并维持员工档案的英文姓氏排序。
- 保留 `EXEC`、`LIST`、`INFO`、`HELP`、`MIAS`、八位代码、`.UFO` 与原型程序命令；自然语言谜语、环境叙事和角色对话使用中文。
- 未发行软件在首次出现时同时显示中文名与英文原名，后续使用中文简称，兼顾理解与版面容量。

### 合集资料与发现链

- 修复开发历史中仍以英文显示其他游戏名的问题，统一改用现行正式译名，包括《忍兵》《涂彩追逐》《莫托尔》《巴布塔》《坎帕内拉》《诡石镇》《瓦尔布雷斯》《罗刹》和《火车劫案》。
- 将未发行项目 `PHOTO MOM` 译为《摄影妈妈》，并统一简介中的 UFO Soft 开发人员中文译名。
- 历史资料中与第 51 款发现链直接相关的 `GREG-MILK` 保持英文，使玩家能够与终端档案和最终输入相互对应。

### 验证与兼容性

- 第 51 款 547/547 条正文通过覆盖、控制符、英文谜题身份、终端协议和输出同步审计。
- 合集元数据重新通过 5918/5918 键审计，821 条认可中文全部写入，非认可字段差异 0、布局字段差异 0。
- 全项目 5195 条带布局字段的文本按 Zpix 8pt/96 DPI 实际字宽审计，预测超过原版最大行数为 0。
- 49 款常规游戏、《诡石镇》、通用菜单和 52 个安装文本重新通过回归检查；本次不修改 `data.win`，沿用 v0.1.2 已验收的绘制补丁。

### 实现

- 新增第 51 款独立译文源、英／日／中对照稿生成器和专用协议审计脚本。
- 扩展元数据构建器与布局审计，使隐藏内容与其余 50 款共用同一套构建和回归流程。
- 公开源码、生成后的中文载荷、安装目录与活动文本保持一致。

## v0.1.2-test - 2026-08-17

### 主要表现修复

- 修复连续中文在原版英文未换行的区域只显示一行或横向溢出的情况；文本现在依据实际绘制框和 Zpix 运行时字宽切行。
- 修复手写逐行绘制仍以固定 8px 纵向步进造成的中文行距重叠、末行裁切及说明栏出框；英文和其他原版语言保持原布局。

### 按游戏

#### 《摇滚岛》

- 开场剧情滚动文本改用真实宽度断行。长篇中文开场会完整分为多行滚动，不再只出现第一行。

#### 《开派对》

- 右侧角色效果、门、通讯录与筹备提示采用 72px 安全宽度，并按中文实际字高排布；较长效果文本不会越出右侧屏幕边界或上下压叠。

#### 《恶魔爆破》《维因格》《坎帕内拉 2》《巴布塔》

- 修复各自显式切行界面仍使用固定 8px 行距的问题；《坎帕内拉 3》的滚动字幕同时补充底部边界判断，避免最后一行被裁掉。

### 实现与文档

- 将《摇滚岛》这类 `scrStringSplit()` 开场与其他显式切行界面纳入同一套实际字体度量规则，而非依赖英文字符数或资源截断字段。

## v0.1.1-test - 2026-08-16

### 中文换行与行距

- 将显式 `string_line_breaks()` 文本框按原版格宽还原为像素宽度，再按 Zpix 8pt 的运行时实际字宽为连续中文断行。
- `draw_text_ext()` 使用调用位置提供的真实绘制宽度自动换行，并采用实际中文行高，修复《虫灾猎手》等子游戏文字出框或上下重叠。
- 不再把资源中的 `*_wc / *_wl` 截断上限误当成全局显示框；只在确知真实宽度的绘制位置断行，避免详情页约 8 字就过早换行。
- 保护连续 `****`、`{0}`、`[1]` 等动态字段，禁止在控制符内部断行；保留常见中文行首、行尾禁则。
- 修复初始化阶段过早调用新增补丁脚本造成的 `rmInit` 崩溃；启动早期换行逻辑完全内联在原有脚本中。
- 按 GameMaker 的 8pt/96 DPI 实际字体度量进行布局审计，而非把字号 8 错当成 8px；4688 条带布局字段的文本预测超限为 0。

### 合集与通用界面排版

- 合集详情恢复原版 160px 文本框，中文约 13 个全角字一行，并按实际字高动态决定行距与每页行数。
- “游玩时间”页改为自适应键值行：同排容得下时左右对齐，容不下时仅该项退为两行，避免固定面板底部出框。
- 档案名称光标改为按当前文本实际宽度定位；名称字符表继续保留原版 8px 固定网格，避免光标错位。
- `draw_text_ce`、`draw_text_centered`、`draw_text_bg_centered` 及主要菜单调用统一按中文实际像素宽度居中，并取消中文分支由 `string_even()` 产生的单边补空格。
- 修复设置菜单、菜单标题、确认窗口、游戏标题、选关标题及带背景文本的中文居中偏移；英文与其他原版字体布局不变。

### 游戏内容与图标

- 修复《鸟族争霸》资源、兵种、建筑与状态字符被显示成 `$`、`%` 或普通字母的问题；ASCII 图标继续使用原版 `fontAvianos`，中文使用 Zpix 定点混排。
- 《虫灾猎手》卡片说明“刷新你的全部模块”精简为“刷新全部模块”。
- `GOLFARIA` 正式译名调整为《高尔夫之歌》。
- `MINI & MAX` 正式译名调整为《小米妮和大麦克》，游戏正文中的人物姓名统一为“小米妮 / 大麦克”。
- `HOT FOOT` 正式译名调整为《烫脚球》，对应简介和教程同步更新。

### 验证与兼容性

- 从干净原版 `data.win` 连续构建两次，结果一致，补丁哈希为 `B9C30A2284E2F384FC9E4058384C84B8E91505E1ABFAFBC7D68174C92A1DC2A9`。
- 50 款子游戏、合集菜单及元数据重新通过键数、控制符、占位符和布局字段审计。
- 《鸟族争霸》77 处混排调用、初始化早期脚本零新增 helper 调用均经反编译回读确认。
- 上述中文居中与新译名已完成实机验收。

## v0.1.0-test - 2026-08-15

- 首次跑通 Windows Steam 版完整汉化安装流程。
- 完成 50 款子游戏、合集菜单、元数据和操作说明的翻译与自校。
- 加入 Zpix 8px 中文字体路由、实际中文字宽与 1px 绘制基线修正。
- 修复旧档案语言回退、`NOSTRING`、选档时间错位、中文标题和详情换行等问题。
- 修复首次启动“铺满”采用错误整数倍率的问题。
- 加入自动定位、联网下载校验、安装备份、完整性校验和安全卸载。
- 加入游戏内汉化署名与非商业用途声明。

这是测试版，不代表最终完成版。后续修订将以 GitHub Issue、PR 和 Release 为准。
